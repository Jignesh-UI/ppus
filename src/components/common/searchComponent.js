import React from "react";
import { SearchSection } from "../../ui-components";

const SearchComponent = () => {
  return (
    <div className="searchSection">
      <SearchSection />
    </div>
  );
};

export default SearchComponent;
